apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: {{ .Values.namespace }}
data:
  MYSQL_DATABASE: {{ .Values.wordpress.db.name | quote }}
  MYSQL_USER: {{ .Values.wordpress.db.user | quote }}
